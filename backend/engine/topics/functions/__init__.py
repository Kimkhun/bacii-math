"""Function-study topic: multi-part BAC II exercises (domain, parity,
one-sided limits, tangent line, derivative of a product, area integral) built
from one function g(x). ``solver.py`` computes every part; ``display.py``
renders the Khmer/English wording; ``generator.py`` replays curated exercises
from ``data/curated/*.json``; ``grader.py`` adds ``grade_graph_check`` (the
hand-drawn-graph label check) on top of the generic core; ``graph_grader.py``/
``graph_renderer.py`` render and Gemini-grade the reference graph drawing.
"""
