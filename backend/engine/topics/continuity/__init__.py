"""Continuity topic: check continuity of a piecewise function at a join
point. ``solver.py`` computes the answer (sets ``answer_kind="continuity"``
so the generic core's ``_judge_continuity`` grades it); ``generator.py``
replays curated exercises from ``data/curated/curated.json``. ``grader.py``
has no custom rule.
"""
