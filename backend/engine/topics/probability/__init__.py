"""Probability topic: scenario-based draw/event exercises (``solver.py`` /
``generator.py``, backed by the user-owned scenario catalog in
``scenarios.py`` + ``data/scenarios/*.json``) and combinatorics counting
(``counting.py``, backed by ``data/counting/*.json``). ``grader.py`` holds
the multi-part grading this topic needs (``grade_multi``,
``parse_multi_answers``, ``split_work_by_part``) that no other topic uses.
"""
