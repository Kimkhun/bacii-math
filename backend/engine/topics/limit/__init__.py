"""Limit topic. ``solver.py`` computes the answer (procedural + curated
branches); ``generator.py`` builds problems (50% curated BAC II exercises,
50% procedural samplers, per technique); ``structures.py`` holds the curated
exercise pool and the technique registry (``LIMIT_TECHNIQUES``) used by the
admin ``/templates`` page. ``grader.py`` has no custom rule.
"""
